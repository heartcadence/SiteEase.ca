
{
  "name": "siteease.ca",
  "version": "1.0.0",
  "description": "High-performance marketing website for SiteEase.",
  "scripts": {
    "build": "mkdir -p dist && tailwindcss -i ./input.css -o ./dist/style.css --minify && cp index.html dist/ && cp -r components types.ts *.tsx dist/"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  }
}
